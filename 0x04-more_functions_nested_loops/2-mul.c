#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: First integer for the multiplicatin function
 * @b: Second iinteger for the the multiplication function
 *
 * Return: result
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
